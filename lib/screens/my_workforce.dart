import 'package:flutter/material.dart';

class MyWorkforceScreen extends StatelessWidget {
  const MyWorkforceScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('My Workforce'),
    );
  }
}
